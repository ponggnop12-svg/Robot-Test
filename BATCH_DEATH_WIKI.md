# Wiki: Batch Death Test

## 📋 ภาพรวม

Test suite นี้ใช้สำหรับทดสอบกระบวนการ Batch Death (ตรวจสอบการเสียชีวิต) ของระบบประกันสังคม โดยจะตรวจสอบข้อมูลก่อนและหลังการรัน batch process และสร้างรายงานผลลัพธ์เป็นไฟล์ Excel

## 🎯 วัตถุประสงค์

- ตรวจสอบข้อมูลผู้ประกันตนที่ว่างงานก่อนรัน batch
- เรียกใช้ batch process สำหรับตรวจสอบการเสียชีวิต
- ตรวจสอบข้อมูลหลังรัน batch
- สร้างรายงานผลการทำงานของ batch

## 📁 ตำแหน่งไฟล์

### ไฟล์ Test
```
E:\program\test-robot\robot-testing-project\tests\database\batch_death_test.robot
```

### โฟลเดอร์ผลลัพธ์
```
E:\program\test-robot\robot-testing-project\results\batch_death\
├── 1.Step1_Before_Death_[timestamp].xlsx
├── 2.Step1_Unemp_After_Death_[timestamp].xlsx
└── 3.Step2_InsurerDeath_[timestamp].xlsx
```

### ไฟล์ Report
```
E:\program\test-robot\robot-testing-project\results\
├── output.xml
├── log.html
└── report.html
```

---

## 🔧 การตั้งค่า

### ข้อมูลการเชื่อมต่อ Database
```
DB_HOST:     <database_host>
DB_PORT:     <database_port>
DB_USER:     <database_user>
DB_PASSWORD: <database_password>
DB_SERVICE:  <database_service>
```

### ข้อมูล Batch API
```
BATCH_API_URL: <batch_api_url>
DEPT_ID:       <department_id>
JOB_NO:        <job_number>
```

## 📝 Test Cases

### TC01: SQL Step1 Before Batch Death
**Tags:** `step1`, `before`, `death`

**วัตถุประสงค์:** ดึงข้อมูลผู้ประกันตนที่ว่างงานก่อนรัน batch

**ผลลัพธ์:**
- ไฟล์ Excel: `1.Step1_Before_Death_[timestamp].xlsx`
- ข้อมูลที่แสดง:
  - เลขประจำตัวประชาชน/เลขที่บัตรประกันสังคม
  - เลขที่รับแจ้ง
  - สถานะการเปลี่ยนแปลง
  - สถานะ Batch และผลการวินิจฉัย
  - วันที่เริ่มมีสิทธิ/สิ้นสุดสิทธิ
  - ข้อมูลการว่างงาน
  - สถานะการมีชีวิตผู้ประกัน
  - วันที่เสียชีวิต

### TC02: Call Batch Check Death
**Tags:** `batch`, `death`

**วัตถุประสงค์:** เรียกใช้ batch process ตรวจสอบการเสียชีวิต

**การทำงาน:**
- เรียก API: `POST /api/job`
- Batch Job: `verifyInsurerDeathMonthlyJob`
- Parameters:
  - departmentIds: <department_id>
  - benefitType: 7
  - businessDate: 2025-02-01

**ผลลัพธ์:**
- Batch process ทำงานสำเร็จ
- ระบบจะสร้าง JOB_NO สำหรับใช้ในขั้นตอนถัดไป

### TC03: SQL Step1_Unemp After Batch Death
**Tags:** `step1_unemp`, `after`, `death`

**วัตถุประสงค์:** ดึงข้อมูลผู้ประกันตนหลังรัน batch

**ผลลัพธ์:**
- ไฟล์ Excel: `2.Step1_Unemp_After_Death_[timestamp].xlsx`
- ข้อมูลเหมือน TC01 เพื่อเปรียบเทียบการเปลี่ยนแปลง

### TC04: SQL Step2_InsurerDeath
**Tags:** `step2`, `death`

**วัตถุประสงค์:** ดึงข้อมูลผลการทำงานของ batch

**ผลลัพธ์:**
- ไฟล์ Excel: `3.Step2_InsurerDeath_[timestamp].xlsx`
- ข้อมูลที่แสดง:
  - ข้อมูลผู้ประกันตนที่เสียชีวิต
  - วันที่เสียชีวิต
  - วันที่รายงานตัวล่าสุด
  - วันเริ่มสิทธิ/วันสิ้นสุดสิทธิของการรายงานตัว
  - สถานะการมีชีวิต
  - สถานะการสร้างงวด (กรณีตาย/ไม่รายงานตัว)
  - ผลการวินิจฉัย
  - เลขที่คำสั่งจ่าย
  - เลขที่ job

## 🚀 ขั้นตอนการรัน

### ⚠️ สำคัญ: ลำดับการรัน Test Cases

**ขั้นตอนที่ 1: รัน TC01 - ดึงข้อมูลก่อน Batch**
```bash
robot --test "TC01 SQL Step1 Before Batch Death" tests/database/batch_death_test.robot
```
- **วัตถุประสงค์:** ดึงข้อมูลผู้ประกันตนที่ว่างงานก่อนรัน Batch
- **ต้องกำหนด:** `${DEPT_ID}` (department_id) ในไฟล์ .robot
- **ผลลัพธ์:** ไฟล์ Excel `1.Step1_Before_Death_[timestamp].xlsx`

**ขั้นตอนที่ 2: รัน Batch Process (ทำด้วยตนเอง หรือใช้ POSTMAN)**

⚠️ **สำคัญ:** ขั้นตอนนี้ต้องรัน Batch ด้วยตนเองผ่านระบบ หรือใช้ POSTMAN/TC02


**วิธีที่ 3: รันผ่านระบบ**
- เข้าระบบ Batch Management
- เลือก Job: verifyInsurerDeathMonthlyJob
- กำหนด Parameters และรัน

**ผลลัพธ์:**
- Batch ทำงานสำเร็จ
- **จดบันทึก `job_no`** ที่ได้จากการรัน (เช่น 12345)
- ใช้ `${DEPT_ID}` เดียวกันกับ TC01

**ขั้นตอนที่ 3: รัน TC03 - ดึงข้อมูลหลัง Batch**
```bash
robot --test "TC03 SQL Step1_Unemp After Batch Death" tests/database/batch_death_test.robot
```
- **วัตถุประสงค์:** ดึงข้อมูลผู้ประกันตนหลังรัน Batch
- **ใช้:** `${DEPT_ID}` เดียวกันกับ TC01
- **ผลลัพธ์:** ไฟล์ Excel `2.Step1_Unemp_After_Death_[timestamp].xlsx`

**ขั้นตอนที่ 4: รัน TC04 - ดึงข้อมูลผลการทำงาน Batch**
```bash
robot --test "TC04 SQL Step2_InsurerDeath" tests/database/batch_death_test.robot
```
- **วัตถุประสงค์:** ดึงข้อมูลผลการทำงานของ Batch
- **ต้องกำหนด:** `${JOB_NO}` (job_no ที่ได้จากขั้นตอนที่ 2) ในไฟล์ .robot
- **ผลลัพธ์:** ไฟล์ Excel `3.Step2_InsurerDeath_[timestamp].xlsx`

### 📝 สรุปลำดับการทำงาน
```
1. TC01 (DEPT_ID) → ไฟล์ Excel ก่อน Batch
2. รัน Batch Process (ด้วยตนเอง/POSTMAN/TC02) → ได้ JOB_NO
3. TC03 (DEPT_ID เดิม) → ไฟล์ Excel หลัง Batch
4. TC04 (JOB_NO) → ไฟล์ Excel ผลการทำงาน
```

### ⚙️ การกำหนดค่าตัวแปร

**ก่อนรัน TC01, TC02 และ TC03:**

เปิดไฟล์ `E:\program\test-robot\robot-testing-project\tests\database\batch_death_test.robot`

แก้ไขบรรทัดที่ 17:
```robot
${DEPT_ID}    1008    # เปลี่ยนเป็น department_id ที่ต้องการ
```

**ตัวอย่างการเปลี่ยน:**
```robot
# ตัวอย่างที่ 1: ทดสอบ สปส. 1006
${DEPT_ID}    1006

# ตัวอย่างที่ 2: ทดสอบ สปส. 1008
${DEPT_ID}    1008

# ตัวอย่างที่ 3: ทดสอบ สปส. 1010
${DEPT_ID}    1010
```

**ก่อนรัน TC04:**

เปิดไฟล์ `E:\program\test-robot\robot-testing-project\tests\database\batch_death_test.robot`

แก้ไขบรรทัดที่ 18:
```robot
${JOB_NO}    9898    # เปลี่ยนเป็น job_no ที่ได้จาก Batch (TC02)
```

**ตัวอย่างการเปลี่ยน:**
```robot
# ตัวอย่างที่ 1: ได้ job_no = 12345 จาก Batch
${JOB_NO}    12345

# ตัวอย่างที่ 2: ได้ job_no = 67890 จาก Batch
${JOB_NO}    67890

# ตัวอย่างที่ 3: ได้ job_no = 99999 จาก Batch
${JOB_NO}    99999
```

### 💡 ตัวอย่างการใช้งานจริง

**สถานการณ์:** ต้องการทดสอบ สปส. 1006 และได้ job_no = 12345 จาก Batch

**ขั้นตอนที่ 1:** แก้ไขไฟล์ `batch_death_test.robot`
```robot
*** Variables ***
${DB_HOST}        <database_host>
${DB_PORT}        <database_port>
${DB_USER}        <database_user>
${DB_PASSWORD}    <database_password>
${DB_SERVICE}     <database_service>
${RESULTS_DIR}    results/batch_death
${DEPT_ID}        1006          # ← เปลี่ยนเป็น 1006
${JOB_NO}         12345         # ← เปลี่ยนเป็น 12345 (หลังรัน TC02)
${BATCH_API_URL}  <batch_api_url>
```

**ขั้นตอนที่ 2:** รัน TC01
```bash
robot --test "TC01 SQL Step1 Before Batch Death" tests/database/batch_death_test.robot
```
→ ได้ไฟล์: `1.Step1_Before_Death_20250201_143022.xlsx`

**ขั้นตอนที่ 3:** รัน TC02
```bash
robot --test "TC02 Call Batch Check Death" tests/database/batch_death_test.robot
```
→ Batch ทำงานสำเร็จ → ได้ job_no = 12345

**ขั้นตอนที่ 4:** แก้ไข `${JOB_NO}` เป็น 12345

**ขั้นตอนที่ 5:** รัน TC03
```bash
robot --test "TC03 SQL Step1_Unemp After Batch Death" tests/database/batch_death_test.robot
```
→ ได้ไฟล์: `2.Step1_Unemp_After_Death_20250201_143022.xlsx`

**ขั้นตอนที่ 6:** รัน TC04
```bash
robot --test "TC04 SQL Step2_InsurerDeath" tests/database/batch_death_test.robot
```
→ ได้ไฟล์: `3.Step2_InsurerDeath_20250201_143022.xlsx`

---

## 🚀 วิธีการรัน (เพิ่มเติม)

### 1. รันทั้งหมด (แนะนำ)
```bash
robot tests/database/batch_death_test.robot
```

### 2. รันเฉพาะ Test Case
```bash
# รันเฉพาะ TC01
robot --test "TC01 SQL Step1 Before Batch Death" tests/database/batch_death_test.robot

# รันเฉพาะ TC02
robot --test "TC02 Call Batch Check Death" tests/database/batch_death_test.robot

# รันเฉพาะ TC03
robot --test "TC03 SQL Step1_Unemp After Batch Death" tests/database/batch_death_test.robot

# รันเฉพาะ TC04
robot --test "TC04 SQL Step2_InsurerDeath" tests/database/batch_death_test.robot
```

### 3. รันตาม Tags
```bash
# รันเฉพาะขั้นตอนก่อน batch
robot --include before tests/database/batch_death_test.robot

# รันเฉพาะ batch process
robot --include batch tests/database/batch_death_test.robot

# รันเฉพาะขั้นตอนหลัง batch
robot --include after tests/database/batch_death_test.robot
```

## 📊 ผลลัพธ์

### ไฟล์ Excel ที่สร้าง
```
E:\program\test-robot\robot-testing-project\results\batch_death\
├── 1.Step1_Before_Death_20250201_143022.xlsx
├── 2.Step1_Unemp_After_Death_20250201_143022.xlsx
└── 3.Step2_InsurerDeath_20250201_143022.xlsx
```

### ไฟล์ Report
```
E:\program\test-robot\robot-testing-project\results\
├── output.xml
├── log.html
└── report.html
```

## 🔍 การตรวจสอบผลลัพธ์

1. **เปรียบเทียบไฟล์ Excel:**
   - เปิดไฟล์ `1.Step1_Before_Death_*.xlsx` และ `2.Step1_Unemp_After_Death_*.xlsx`
   - ตรวจสอบการเปลี่ยนแปลงของสถานะการมีชีวิต
   - ตรวจสอบวันที่เสียชีวิต

2. **ตรวจสอบผลการทำงาน:**
   - เปิดไฟล์ `3.Step2_InsurerDeath_*.xlsx`
   - ตรวจสอบ:
     - วันที่เสียชีวิต
     - สถานะการสร้างงวด
     - เลขที่คำสั่งจ่าย
     - ผลการวินิจฉัย

3. **ตรวจสอบ Log:**
   - เปิดไฟล์ `results/log.html`
   - ดูรายละเอียดการทำงานแต่ละขั้นตอน

## ⚙️ การปรับแต่ง

### เปลี่ยน Department ID
แก้ไขในไฟล์ `batch_death_test.robot`:
```robot
${DEPT_ID}    <department_id>    # เปลี่ยนเป็น สปส. ที่ต้องการ
```

### เปลี่ยน Job Number
แก้ไขในไฟล์ `batch_death_test.robot`:
```robot
${JOB_NO}    <job_number>    # เปลี่ยนเป็น job_no ที่ได้จาก Batch
```

### เปลี่ยน Business Date
แก้ไขใน keyword `Run Batch Death Process`:
```robot
${batch_body}=    Create Dictionary
...    businessDate=2025-02-01    # เปลี่ยนวันที่ตามต้องการ
```

### เปลี่ยน Benefit Type
แก้ไขใน keyword `Run Batch Death Process`:
```robot
${addition_params}=    Create Dictionary
...    benefitType=7    # เปลี่ยนประเภทสิทธิประโยชน์
```

## 📋 ข้อกำหนดเบื้องต้น

### Python Libraries
```bash
pip install robotframework
pip install robotframework-databaselibrary
pip install robotframework-requests
pip install oracledb
pip install openpyxl
```

### การเข้าถึง
- เชื่อมต่อ Database Oracle ได้
- เข้าถึง Batch API ได้
- สิทธิ์ในการเขียนไฟล์ในโฟลเดอร์ results/

## ⚠️ ข้อควรระวัง

1. **Database Connection:** ตรวจสอบว่าสามารถเชื่อมต่อ Database ได้
2. **API Endpoint:** ตรวจสอบว่า API URL ถูกต้องและเข้าถึงได้
3. **Batch Process:** การรัน batch อาจใช้เวลานาน ควรรอให้เสร็จสมบูรณ์
4. **ข้อมูลทดสอบ:** ควรใช้ข้อมูลทดสอบ ไม่ใช่ข้อมูลจริง
5. **Timestamp:** ไฟล์ Excel จะมี timestamp เพื่อไม่ให้ซ้ำกัน
6. **JOB_NO:** ต้องแก้ไข JOB_NO หลังจากรัน TC02 ก่อนรัน TC04

## 🐛 การแก้ไขปัญหา

### ปัญหา: เชื่อมต่อ Database ไม่ได้
```
ตรวจสอบ:
- DB_HOST, DB_PORT ถูกต้อง
- User/Password ถูกต้อง
- Network เชื่อมต่อได้
```

### ปัญหา: Batch API ไม่ตอบสนอง
```
ตรวจสอบ:
- BATCH_API_URL ถูกต้อง
- Network เชื่อมต่อได้
- API Service ทำงานปกติ
```

### ปัญหา: ไม่สามารถสร้างไฟล์ Excel
```
ตรวจสอบ:
- โฟลเดอร์ results/batch_death/ มีอยู่
- มีสิทธิ์เขียนไฟล์
- ติดตั้ง openpyxl แล้ว
```

## 📞 ติดต่อ

หากมีปัญหาหรือข้อสงสัย กรุณาติดต่อทีมพัฒนา
