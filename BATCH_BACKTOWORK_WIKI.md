# Wiki: Batch BackToWork Test

## 📋 ภาพรวม

Test suite นี้ใช้สำหรับทดสอบกระบวนการ Batch BackToWork (กลับเข้าทำงาน) ของระบบประกันสังคม โดยจะตรวจสอบข้อมูลก่อนและหลังการรัน batch process และสร้างรายงานผลลัพธ์เป็นไฟล์ Excel

## 🎯 วัตถุประสงค์

- ตรวจสอบข้อมูลผู้ประกันตนที่ว่างงานก่อนรัน batch
- เรียกใช้ batch process สำหรับกลับเข้าทำงาน
- ตรวจสอบข้อมูลหลังรัน batch
- สร้างรายงานผลการทำงานของ batch

## 🔧 การตั้งค่า

### ข้อมูลการเชื่อมต่อ Database
```
DB_HOST:     <database_host>
DB_PORT:     <database_port>
DB_USER:     <database_user>
DB_PASSWORD: <database_password>
DB_SERVICE:  <database_service>
```

### ข้อมูล Batch API
```
BATCH_API_URL: <batch_api_url>
DEPT_ID:       <department_id>
```

### โฟลเดอร์ผลลัพธ์
```
results/batch_backtowork/
```

## 📝 Test Cases

### TC01: SQL Step1 Before Batch BackToWork
**Tags:** `step1`, `before`, `backtowork`

**วัตถุประสงค์:** ดึงข้อมูลผู้ประกันตนที่ว่างงานก่อนรัน batch

**ผลลัพธ์:**
- ไฟล์ Excel: `1.Step1_Before_BackToWork_[timestamp].xlsx`
- ข้อมูลที่แสดง:
  - เลขประจำตัวประชาชน/เลขที่บัตรประกันสังคม
  - เลขที่รับแจ้ง
  - สถานะการเปลี่ยนแปลง
  - สถานะ Batch และผลการวินิจฉัย
  - วันที่เริ่มมีสิทธิ/สิ้นสุดสิทธิ
  - อัตราการจ่ายประโยชน์ทดแทน
  - ข้อมูลการว่างงาน
  - ข้อมูลการชำระเงิน
  - สถานะการมีชีวิตผู้ประกัน

### TC02: Call Batch BackToWork
**Tags:** `batch`, `backtowork`

**วัตถุประสงค์:** เรียกใช้ batch process กลับเข้าทำงาน

**การทำงาน:**
- เรียก API: `POST /api/job`
- Batch Job: `verifyBackToWorkMonthlyJob`
- Parameters:
  - departmentIds: 1008
  - benefitType: 7
  - businessDate: 2025-02-01

**ผลลัพธ์:**
- ได้รับ JOB_NO สำหรับใช้ในขั้นตอนถัดไป

### TC03: SQL Step1_Unemp After Batch BackToWork
**Tags:** `step1_unemp`, `after`, `backtowork`

**วัตถุประสงค์:** ดึงข้อมูลผู้ประกันตนหลังรัน batch

**ผลลัพธ์:**
- ไฟล์ Excel: `2.Step1_Unemp_After_BackToWork_[timestamp].xlsx`
- ข้อมูลเหมือน TC01 เพื่อเปรียบเทียบการเปลี่ยนแปลง

### TC04: SQL Step3_BackToWork
**Tags:** `step3`, `backtowork`

**วัตถุประสงค์:** ดึงข้อมูลผลการทำงานของ batch

**ผลลัพธ์:**
- ไฟล์ Excel: `3.Step3_BackToWork_[timestamp].xlsx`
- ข้อมูลที่แสดง:
  - ข้อมูลผู้ประกันตนที่กลับเข้าทำงาน
  - วันที่กลับเข้าทำงาน
  - ข้อมูลสถานประกอบการ
  - สถานะการสร้างงวด
  - เลขที่คำสั่งจ่าย
  - เลขที่ job และวันที่ประมวลผล

## 🚀 ขั้นตอนการรัน

### ⚠️ สำคัญ: ลำดับการรัน Test Cases

**ขั้นตอนที่ 1: รัน TC01 - ดึงข้อมูลก่อน Batch**
```bash
robot --test "TC01 SQL Step1 Before Batch BackToWork" tests/database/batch_backtowork_test.robot
```
- **วัตถุประสงค์:** ดึงข้อมูลผู้ประกันตนที่ว่างงานก่อนรัน Batch
- **ต้องกำหนด:** `${DEPT_ID}` (department_id) ในไฟล์ .robot
- **ผลลัพธ์:** ไฟล์ Excel `1.Step1_Before_BackToWork_[timestamp].xlsx`

**ขั้นตอนที่ 2: รัน Batch Process (ทำด้วยตนเอง)**
- เรียก Batch API หรือรันผ่านระบบ
- **จดบันทึก `job_no`** ที่ได้จาก Batch
- ใช้ `${DEPT_ID}` เดียวกันกับ TC01

**ขั้นตอนที่ 3: รัน TC03 - ดึงข้อมูลหลัง Batch**
```bash
robot --test "TC03 SQL Step1_Unemp After Batch BackToWork" tests/database/batch_backtowork_test.robot
```
- **วัตถุประสงค์:** ดึงข้อมูลผู้ประกันตนหลังรัน Batch
- **ใช้:** `${DEPT_ID}` เดียวกันกับ TC01
- **ผลลัพธ์:** ไฟล์ Excel `2.Step1_Unemp_After_BackToWork_[timestamp].xlsx`

**ขั้นตอนที่ 4: รัน TC04 - ดึงข้อมูลผลการทำงาน Batch**
```bash
robot --test "TC04 SQL Step3_BackToWork" tests/database/batch_backtowork_test.robot
```
- **วัตถุประสงค์:** ดึงข้อมูลผลการทำงานของ Batch
- **ต้องกำหนด:** `${JOB_NO}` (job_no ที่ได้จากขั้นตอนที่ 2) ในไฟล์ .robot
- **ผลลัพธ์:** ไฟล์ Excel `3.Step3_BackToWork_[timestamp].xlsx`

### 📝 สรุปลำดับการทำงาน
```
1. TC01 (DEPT_ID) → ไฟล์ Excel ก่อน Batch
2. รัน Batch Process → ได้ JOB_NO
3. TC03 (DEPT_ID เดิม) → ไฟล์ Excel หลัง Batch
4. TC04 (JOB_NO) → ไฟล์ Excel ผลการทำงาน
```

### ⚙️ การกำหนดค่าตัวแปร

**ก่อนรัน TC01 และ TC03:**

เปิดไฟล์ `batch_backtowork_test.robot` และแก้ไขบรรทัดที่ 17:
```robot
${DEPT_ID}    1008    # เปลี่ยนเป็น department_id ที่ต้องการ
```

**ตัวอย่างการเปลี่ยน:**
```robot
# ตัวอย่างที่ 1: ทดสอบ สปส. 1006
${DEPT_ID}    1006

# ตัวอย่างที่ 2: ทดสอบ สปส. 1008
${DEPT_ID}    1008

# ตัวอย่างที่ 3: ทดสอบ สปส. 1010
${DEPT_ID}    1010
```

**ก่อนรัน TC04:**

เปิดไฟล์ `batch_backtowork_test.robot` และแก้ไขบรรทัดที่ 19:
```robot
${JOB_NO}    9912    # เปลี่ยนเป็น job_no ที่ได้จาก Batch
```

**ตัวอย่างการเปลี่ยน:**
```robot
# ตัวอย่างที่ 1: ได้ job_no = 12345 จาก Batch
${JOB_NO}    12345

# ตัวอย่างที่ 2: ได้ job_no = 67890 จาก Batch
${JOB_NO}    67890

# ตัวอย่างที่ 3: ได้ job_no = 99999 จาก Batch
${JOB_NO}    99999
```

### 💡 ตัวอย่างการใช้งานจริง

**สถานการณ์:** ต้องการทดสอบ สปส. 1006 และได้ job_no = 12345 จาก Batch

**ขั้นตอนที่ 1:** แก้ไขไฟล์ `batch_backtowork_test.robot`
```robot
*** Variables ***
${DB_HOST}        <database_host>
${DB_PORT}        <database_port>
${DB_USER}        <database_user>
${DB_PASSWORD}    <database_password>
${DB_SERVICE}     <database_service>
${RESULTS_DIR}    results/batch_backtowork
${DEPT_ID}        1006          # ← เปลี่ยนเป็น 1006
${BATCH_API_URL}  <batch_api_url>
${JOB_NO}         12345         # ← เปลี่ยนเป็น 12345 (หลังรัน Batch)
```

**ขั้นตอนที่ 2:** รัน TC01
```bash
robot --test "TC01 SQL Step1 Before Batch BackToWork" tests/database/batch_backtowork_test.robot
```
→ ได้ไฟล์: `1.Step1_Before_BackToWork_20250201_143022.xlsx`

**ขั้นตอนที่ 3:** รัน Batch Process (ด้วยตนเอง)
→ ได้ job_no = 12345

**ขั้นตอนที่ 4:** แก้ไข `${JOB_NO}` เป็น 12345 (ถ้ายังไม่ได้แก้)

**ขั้นตอนที่ 5:** รัน TC03
```bash
robot --test "TC03 SQL Step1_Unemp After Batch BackToWork" tests/database/batch_backtowork_test.robot
```
→ ได้ไฟล์: `2.Step1_Unemp_After_BackToWork_20250201_143022.xlsx`

**ขั้นตอนที่ 6:** รัน TC04
```bash
robot --test "TC04 SQL Step3_BackToWork" tests/database/batch_backtowork_test.robot
```
→ ได้ไฟล์: `3.Step3_BackToWork_20250201_143022.xlsx`

---

## 🚀 วิธีการรัน (เพิ่มเติม)

### 1. รันทั้งหมด (แนะนำ)
```bash
robot tests/database/batch_backtowork_test.robot
```

### 2. รันเฉพาะ Test Case
```bash
# รันเฉพาะ TC01
robot --test "TC01 SQL Step1 Before Batch BackToWork" tests/database/batch_backtowork_test.robot

# รันเฉพาะ TC02
robot --test "TC02 Call Batch BackToWork" tests/database/batch_backtowork_test.robot
```

### 3. รันตาม Tags
```bash
# รันเฉพาะขั้นตอนก่อน batch
robot --include before tests/database/batch_backtowork_test.robot

# รันเฉพาะ batch process
robot --include batch tests/database/batch_backtowork_test.robot

# รันเฉพาะขั้นตอนหลัง batch
robot --include after tests/database/batch_backtowork_test.robot
```

### 4. รันด้วย Python Script
```bash
python run_tests.py --type database
```

## 📊 ผลลัพธ์

### ไฟล์ Excel ที่สร้าง
```
results/batch_backtowork/
├── 1.Step1_Before_BackToWork_20250201_143022.xlsx
├── 2.Step1_Unemp_After_BackToWork_20250201_143022.xlsx
└── 3.Step3_BackToWork_20250201_143022.xlsx
```

## 🔍 การตรวจสอบผลลัพธ์

1. **เปรียบเทียบไฟล์ Excel:**
   - เปิดไฟล์ `1.Step1_Before_BackToWork_*.xlsx` และ `2.Step1_Unemp_After_BackToWork_*.xlsx`
   - ตรวจสอบการเปลี่ยนแปลงของสถานะ

2. **ตรวจสอบผลการทำงาน:**
   - เปิดไฟล์ `3.Step3_BackToWork_*.xlsx`
   - ตรวจสอบ:
     - วันที่กลับเข้าทำงาน
     - สถานะการสร้างงวด
     - เลขที่คำสั่งจ่าย

3. **ตรวจสอบ Log:**
   - เปิดไฟล์ `results/log.html`
   - ดูรายละเอียดการทำงานแต่ละขั้นตอน

## ⚙️ การปรับแต่ง

### เปลี่ยน Department ID
แก้ไขในไฟล์ `batch_backtowork_test.robot`:
```robot
${DEPT_ID}    <department_id>    # เปลี่ยนเป็น สปส. ที่ต้องการ
```

### เปลี่ยน Business Date
แก้ไขใน keyword `Run Batch BackToWork Process`:
```robot
${batch_body}=    Create Dictionary
...    businessDate=2025-02-01    # เปลี่ยนวันที่ตามต้องการ
```

### เปลี่ยน Benefit Type
แก้ไขใน keyword `Run Batch BackToWork Process`:
```robot
${addition_params}=    Create Dictionary
...    benefitType=7    # เปลี่ยนประเภทสิทธิประโยชน์
```

## 📋 ข้อกำหนดเบื้องต้น

### Python Libraries
```bash
pip install robotframework
pip install robotframework-databaselibrary
pip install oracledb
pip install openpyxl
```